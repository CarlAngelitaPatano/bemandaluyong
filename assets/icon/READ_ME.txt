Save the app icon image here as:

  app_icon.png

Use the City of Mandaluyong seal (square PNG, ideally 1024x1024).
After saving it, run:

  flutter pub add dev:flutter_launcher_icons
  dart run flutter_launcher_icons
  flutter run

That generates the launcher icon for the app.
